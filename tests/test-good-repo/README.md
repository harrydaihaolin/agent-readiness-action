# test-good-repo

Tiny known-good repo used by `agent-readiness-action`'s CI to assert
the action runs end-to-end and produces a non-trivial score. Intentionally
minimal — adding files here changes the smoke-test baseline.
