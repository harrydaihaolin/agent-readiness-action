# AGENTS.md — test-good-repo

A minimal sample repo used to smoke-test `agent-readiness-action`.

## Canonical commands

| Task | Command |
|---|---|
| Test | `make test` |
| Run | `python -m good.cli` |

## Do-not-touch

- `Makefile` targets are referenced by the parent action's CI matrix.
